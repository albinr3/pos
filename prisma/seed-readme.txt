Seed info:
- Creates company settings, invoice sequence 'A', generic customer, and admin user.
- admin credentials: username=admin password=admin

Note: password hashing uses bcrypt (as in seed.ts). Tune cost and policy for production.
